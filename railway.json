{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"[Deploy] Starting TradeUp v2.0.4...\" && echo \"[Deploy] Skipping migrations (using fix-schema endpoint)...\" && gunicorn -c gunicorn.conf.py run:app'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}

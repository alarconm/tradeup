{
  "name": "tradeup-tier-discount-function",
  "version": "1.0.0",
  "description": "Shopify Function for TradeUp tier-based automatic discounts",
  "type": "module",
  "scripts": {
    "build": "shopify app function build",
    "preview": "shopify app function run",
    "test": "vitest run",
    "test:watch": "vitest",
    "typegen": "shopify app function typegen"
  },
  "dependencies": {
    "@shopify/shopify_function": "^1.0.0"
  },
  "devDependencies": {
    "@shopify/prettier-config": "^1.1.2",
    "prettier": "^3.2.0",
    "vitest": "^1.2.0"
  },
  "prettier": "@shopify/prettier-config",
  "engines": {
    "node": ">=18.0.0"
  }
}

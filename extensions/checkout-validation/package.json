{
  "name": "tradeup-checkout-validation",
  "version": "1.0.0",
  "description": "TradeUp checkout validation function for points redemption and tier-restricted products",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "shopify app function build",
    "preview": "shopify app function run",
    "test": "shopify app function run --input test-input.json",
    "typegen": "shopify app function typegen"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "shopify",
    "functions",
    "checkout",
    "validation",
    "tradeup",
    "loyalty",
    "points"
  ],
  "dependencies": {
    "@shopify/shopify_function": "^1.0.0"
  },
  "devDependencies": {
    "@shopify/prettier-config": "^1.1.2",
    "prettier": "^3.2.0"
  }
}
